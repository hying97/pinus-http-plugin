export declare class PinusEvent {
    constructor(app: any, opts: any);
    add_servers(servers: any): void;
    remove_servers(ids: any): void;
    replace_servers(servers: any): void;
    bind_session(session: any): void;
    close_session(session: any): void;
}
